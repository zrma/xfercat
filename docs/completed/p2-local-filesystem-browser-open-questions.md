# Resolved And Deferred Questions

non-Unicode path와 symlink transfer는 정확한 cross-platform identity contract가 필요하므로
현재 browser에서 제외한다. macOS는 invalid UTF-8 filename 생성을 filesystem level에서
거부하고 Linux fixture에서만 해당 exclusion regression을 실행한다.
