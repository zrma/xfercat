# Open Questions

## Runtime And Interface

- TUI가 connection picker와 Waybill의 focus/state를 충분히 명확하게 표현하는가?
- GUI가 제공하는 discoverability가 packaging과 platform integration 비용을 정당화하는가?
- 한 runtime으로 core와 interface를 구성할지 domain core를 별도 library로 둘지?

## Transport

- 첫 network slice는 SFTP만 지원해도 product promise를 검증할 수 있는가?
- OpenSSH config와 agent를 직접 참조할 때 필요한 interoperability 범위는 무엇인가?

## Persistence

- process 종료 뒤에도 draft plan을 유지해야 하는가?
- persistence가 필요하다면 endpoint와 path를 어떤 privacy boundary로 저장할 것인가?
